# -*- mode: snippet -*-
# name: Rust display implementation
# key: idisplay
# --
impl Display for $1 {
    fn fmt(&self, f: &mut Formatter<'_>) -> std::fmt::Result {
        write!(f, "{}", $0)
    }
}